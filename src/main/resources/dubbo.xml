<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xmlns:context="http://www.springframework.org/schema/context"
    xmlns:dubbo="http://code.alibabatech.com/schema/dubbo"
    xsi:schemaLocation="http://www.springframework.org/schema/beans 
    					http://www.springframework.org/schema/beans/spring-beans.xsd 
    					http://www.springframework.org/schema/context 
						http://www.springframework.org/schema/context/spring-context.xsd
    					http://code.alibabatech.com/schema/dubbo 
    					http://code.alibabatech.com/schema/dubbo/dubbo.xsd">
 
    <!-- 消费方应用名，用于计算依赖关系，不是匹配条件，不要与提供方一样 -->
    <dubbo:application name="mcd-consumer" />
 
	<context:component-scan base-package="com.asiainfo.biapp.mcd" />

    <!-- 注册中心地址 -->
	<dubbo:registry protocol="zookeeper" address="${mcd.ec.dubbo.comsumer.zookeeper}" />

	<dubbo:reference id="sendServiceImpl" interface="com.asiainfo.biapp.mcd.sms.service.ISendService" 
		timeout="600000" check="false"/>

    <dubbo:reference id="filterService" interface="com.asiainfo.biapp.mcd.fqc.service.IFrequencyFilterService" 
    	timeout="600000" check="false" />
</beans>